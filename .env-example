# ==========================================
# 🔒 ARQUIVO DE VARIÁVEIS DE AMBIENTE (.env)
# NUNCA COMMITE ESTE ARQUIVO NO GITHUB!
# Copie este arquivo para .env e configure suas variáveis
# ==========================================

DB_HOST=YOUR_HOST
DB_PORT=YOUR_PORT
DB_NAME=YOUR_DB_NAME
DB_USER=YOUR_USER
DB_PASS=YOUR_PASSWORD
DB_SSL=true

# --- Segurança (JWT) ---
# ✅ CORREÇÃO CRÍTICA: Gere um secret de 256 bits em produção
# Exemplo de geração: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_256_BIT_STRING_IN_PRODUCTION

# --- Integrações Externas ---
# Sua chave da API do Google Cloud
GOOGLE_API_KEY=YOUR_API_KEY

# --- ✅ NOVO: CORS ---
# Origens permitidas (separadas por vírgula)
# Desenvolvimento: http://localhost:3000
# Produção: https://meusite.com,https://www.meusite.com
ALLOWED_ORIGINS=http://localhost:3000